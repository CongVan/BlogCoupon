@using BlogCoupon.Models
@model List<Post>
@{
    ViewBag.Title = "Trang chủ";
    Layout = "~/Views/Shared/HomeLayout.cshtml";
}

<!--Section: Blog v.3-->
<section class="section extra-margins pb-3 text-center text-lg-left pt-0">

    <!--Section heading-->
    <h2 class=" text-primary border border-primary border-left-0 border-top-0 border-right-0 pb-1"> <i class="fa fa-tags"></i>Coupon và mã giảm giá </h2>
    <!--Section description-->
    @* <p class="section-description">Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.</p>*@

    <!--Grid row-->
    @foreach (var item in Model)
    {
        <div class="row hoverable ">

            <!--Grid column-->
            <div class="col-lg-4 mb-4 py-2 px-1 pt-4">
                <!--Featured image-->
                <div class="view overlay hm-white-slight z-depth-1-half">
                    <img src="@item.ImgThumb" class="img-fluid" alt="@item.Title">
                    <a href="@Url.RouteUrl("PageBySlug", new { slug=item.Slug })">
                        <div class="mask"></div>
                    </a>
                </div>
            </div>
            <!--Grid column-->
            <!--Grid column-->
            <div class="col-lg-8 mb-1 pt-4">
                <!--Excerpt-->
                <a class="mb-3" href="@(String.IsNullOrEmpty(item.Slug)?"#":@Url.RouteUrl("PageBySlug", new { slug=item.Slug }))"><h4><strong>@item.Title</strong></h4></a>
                <p class="black-text" style="font-weight: bolder;">@item.Intro</p>
                <p class="black-text" style="font-weight: bolder;"><a><strong><i class="fa fa-pencil"></i> Cao Hiếu</strong></a> | <i class="fa fa-calendar"></i> @(item.CreateDate == null ? "" : item.CreateDate.Value.Date.ToString("dd/MM/yyyy") )</p>
                <p class="flex-row">
                    <a  href="" class="teal-text">
                        <small class="text-danger"><i class="fa fa-heart"></i><strong> Danh mục </strong></small>
                    </a>
                    &nbsp;&nbsp;
                    <small class="text-muted"><i class="fa fa-comment"></i>100,&nbsp; <i class="fa fa-eye"></i>100</small>   
                </p>
               
               
              
                
             @*<a class="btn btn-primary btn-md" href="@Url.RouteUrl("PageBySlug", new { slug=item.Slug })">Xem chi tiết</a>*@
            </div>
            <!--Grid column-->

        </div>
        <hr class="mb-1" style="display:block!important">
    }

    <!--Grid row-->



</section>
<!--Section: Blog v.3-->
<!-- Provides extra visual weight and identifies the primary action in a set of buttons -->
<button type="button" class="btn btn-primary">Primary</button>

<!-- Default button -->
<button type="button" class="btn btn-default">Default</button>

<!-- Secondary button -->
<button type="button" class="btn btn-secondary">Secondary</button>

<!-- Indicates a successful or positive action -->
<button type="button" class="btn btn-success">Success</button>

<!-- Contextual button for informational alert messages -->
<button type="button" class="btn btn-info">Info</button>

<!-- Indicates caution should be taken with this action -->
<button type="button" class="btn btn-warning">Warning</button>

<!-- Indicates a dangerous or potentially negative action -->
<button type="button" class="btn btn-danger">Danger</button>


<!-- Button trigger modal -->
<button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#basicExample">
    Launch demo modal
</button>

<!-- Modal -->
<!-- Modal -->
<div class="row">

</div>





@section js{
    <script>
        $.ajax({
            type: "get",
            dataType:"json",
            headers: { "Content-Type": "application/json", "Access-Control-Allow-Origin":"*", "Authorization": "Token 4rIpCCX60Y1PMM_LLlXhVOUqCpmqRCfe" },        
            url:"https://api.accesstrade.vn/v1/transactions?since=2016-08-01T00:00:00Z&until=2016-08-02T00:00:00Z",
            contentType: "application/json",
            //crossDomain: true,
            success: function (data) {
                console.log(data);
            },
            error: function (jqXHR, textStatus, ex) {
               // console.log(status);
                alert(textStatus + "," + ex + "," + jqXHR.responseText);

            }
        });

    </script>    
}